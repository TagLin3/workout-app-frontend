import { Box, Typography } from "@mui/material";

/* eslint max-len: 0 */
const Home = () => (
  <Box>
    <Typography variant="h1">Application for planning workouts and logging them</Typography>
    <Typography>This is the (unfinished) homepage. Click &quot;login&quot; at the top to login or &quot;register&quot; if you aren&apos;t registered yet</Typography>
  </Box>
);

export default Home;
