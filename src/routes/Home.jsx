import { Typography } from "@mui/material";

/* eslint max-len: 0 */
const Home = () => (
  <div>
    <h1>Application for planning workouts and logging them</h1>
    <p>This is the (unfinished) homepage. Click &quot;login&quot; at the top to login or &quot;register&quot; if you aren&apos;t registered yet</p>
    <Typography>test</Typography>
  </div>
);

export default Home;
